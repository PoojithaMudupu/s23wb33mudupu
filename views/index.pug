extends layout

block content
  h1= title
  p Welcome to PoojithaMudupu
  p I love to make films
  button(type="button", class="btn btn-info" onclick="doReduce()") Press Me
  a(href='http://localhost:3000/movies') Link
  div#reduce Reduce the value
  script.
    let count = 20;
    let change = -1;
    function doReduce() {
      count += change;
      change -= 1;
      document.getElementById('reduce').textContent = `Count ${count} with change ${change}`;
    }